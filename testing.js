var requests = [1];
var requestLenght = requests.length;
var noTickets = false;
if (requestLenght == 0) {
    noTickets = true;
    console.log(noTickets);
} else {
    noTickets;
    console.log(noTickets);
}
